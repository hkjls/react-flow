const FreeSim =({n, setN}: any)=>{

    const node_list=()=>{
        setN([])
        }

    return(
        <div onClick={node_list}>
            Simulation Libre
        </div>
    )
}

export default FreeSim